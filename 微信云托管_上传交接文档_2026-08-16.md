# 微信小程序版：代码交接与上传部署说明

> 交接日期：2026-08-16  
> 文档用途：给负责 Git 上传、GitHub 公开、微信云托管部署的技术人员  
> 代码维护分工：本文档作者只负责写代码；上传、公开、部署由运营/部署技术员执行

---

## 1. 仓库基本信息

```text
本地仓库目录：D:\chuangxinchaungye\奶山羊日粮配比助手_源码_2026-08-07
远程仓库：https://github.com/sjeqiqi/-.git
分支：main
当前版本：0.1.0
```

本仓库包含：

- `backend/`：FastAPI 后端，营养模型、优化器、AI 回退均已实现。
- `frontend/`：现有 React H5 前端，后续可改造为微信小程序前端。
- `交接文档.md`：原始项目交接说明，测试基线为后端 75 项、前端 16 项。
- `Dockerfile` / `.dockerignore`：本次新增，用于微信云托管容器部署。

---

## 2. 本次代码改动说明

代码侧已由开发者完成以下改动：

1. `README.md`
   - 测试数量已从旧值更新为：后端 75 个、前端 16 个。
   - AI 密钥说明已改为“后端只读进程环境变量，当前不会自动解析 `.env`”。

2. `.env.example`
   - 已改为环境变量注入说明，避免误以为复制 `.env` 后会自动生效。

3. `frontend/src/types.ts`
   - `FeasibleRation` 类型补齐 `qualified: true`。

4. `frontend/src/__tests__/App.test.tsx`
   - 测试 fixture 同步补齐 `qualified: true`。

5. 新增 `Dockerfile`
   - 用于微信云托管容器化运行 FastAPI 后端。
   - 监听端口：`8000`。
   - 健康检查路径：`/api/health`。

6. 新增 `.dockerignore`
   - 排除 Git 目录、`.env`、测试缓存、前端源码和文档，避免进入 Docker 构建上下文。

> 注意：仓库中在本次改动前还存在一批尚未提交的源码和文档改动。  
> 上传前必须由上传技术员完整查看 `git status` 和 `git diff`，确认哪些文件属于本次应提交内容。

---

## 3. 安全红线

### 3.1 `.env` 绝对不能提交

本地项目根目录存在 `.env`，里面可能含有非空 `DEEPSEEK_API_KEY`。

- `.gitignore` 已经忽略 `.env`。
- 正常情况下执行 `git add .` 不会添加 `.env`。
- **禁止执行 `git add -f .env`。**
- **禁止把 `.env` 内容复制到 README、代码、测试、Dockerfile 或提交信息中。**

### 3.2 公开前必须检查密钥是否曾被提交

在 PowerShell 中执行：

```powershell
cd D:\chuangxinchaungye\奶山羊日粮配比助手_源码_2026-08-07
git ls-files .env
```

- 没有输出：说明 `.env` 从未被 Git 跟踪，安全。
- 有输出：说明 `.env` 曾被提交，必须立即执行：

```powershell
git rm --cached .env
git commit -m "chore: remove .env from version control"
git push origin main
```

然后联系 API 提供方**轮换 DeepSeek API Key**，因为公开仓库的历史记录可能已经泄露该密钥。

---

## 4. 上传技术员操作步骤

### 4.1 打开项目目录的 PowerShell

1. 打开文件资源管理器。
2. 进入：

```text
D:\chuangxinchaungye\奶山羊日粮配比助手_源码_2026-08-07
```

3. 在地址栏输入 `powershell` 并回车。

### 4.2 查看工作区状态

```powershell
cd D:\chuangxinchaungye\奶山羊日粮配比助手_源码_2026-08-07
git status
git diff --stat
git diff --check
```

需要重点检查：

- `modified:` 和 `Untracked files:` 中，是否只有源码、测试、文档、Dockerfile。
- `frontend/dist`、`node_modules`、`__pycache__`、`.env` 不应出现在提交列表中。

### 4.3 查看具体改动

```powershell
git diff
```

如有新增未跟踪文件，可以单独查看：

```powershell
Get-Content Dockerfile
Get-Content .dockerignore
```

### 4.4 提交并推送

确认全部改动都是本次要发布的内容后，执行：

```powershell
git add .
git status
git commit -m "feat: prepare wechat miniprogram cloud hosting deployment"
git push -u origin main
```

如果 `push` 提示需要登录：

- 浏览器弹窗则选择授权 GitHub。
- 未弹窗则按 GitHub 要求使用 Personal Access Token 或 GitHub CLI 登录。

### 4.5 将 GitHub 仓库设为公开

用仓库 owner 的 GitHub 账号打开：

```text
https://github.com/sjeqiqi/-/settings
```

页面底部：

```text
Danger Zone → Change repository visibility → Change to public
```

按提示输入仓库名 `-` 确认。

也可以使用 GitHub CLI：

```powershell
gh repo edit sjeqiqi/- --visibility public
```

---

## 5. 微信云托管部署步骤

### 5.1 前置条件

- 已有微信小程序 AppID。
- GitHub 账号已绑定到微信云托管。
- 仓库已推送到 GitHub 并设为公开或已授权云托管访问私有仓库。

### 5.2 创建云托管服务

1. 登录微信云托管控制台。
2. 新建服务。
3. 代码来源选择 **GitHub**。
4. 授权并选择仓库：

```text
sjeqiqi/-
```

5. 分支选择：

```text
main
```

6. 部署方式选择 **Dockerfile**，路径使用：

```text
./Dockerfile
```

7. 服务端口填写：

```text
8000
```

### 5.3 环境变量

在云托管控制台的服务设置中添加：

```text
DEEPSEEK_API_KEY = 真实密钥
```

注意：

- 如果不配置该变量，后端 AI 接口会自动返回本地固定说明，不影响配方计算。
- 密钥只能配置在云托管环境变量中，不能写入 Dockerfile、小程序代码或仓库。

### 5.4 健康检查

健康检查路径填写：

```text
/api/health
```

预期返回：

```json
{"status":"ok","service":"dairy-goat-ration-mvp","version":"0.1.0"}
```

### 5.5 配置小程序服务器域名

拿到云托管提供的 HTTPS 默认域名后，登录微信公众平台：

```text
开发管理 → 开发设置 → 服务器域名
```

把该 HTTPS 域名加入：

```text
request 合法域名
```

微信小程序生产环境只允许 HTTPS 请求。

---

## 6. 部署完成后的验证

以下命令在 PowerShell 中执行，将 `https://你的域名` 替换为云托管实际域名。

### 6.1 健康检查

```powershell
Invoke-RestMethod -Uri "https://你的域名/api/health" -Method Get
```

### 6.2 原料目录

```powershell
Invoke-RestMethod -Uri "https://你的域名/api/feeds" -Method Get
```

预期返回 13 种原料和 `version: "v1"`。

### 6.3 正常配方计算

```powershell
$body = @{
  animal = @{
    class = "lactating"
    body_weight_kg = 50
    milk_kg = 2.5
    milk_fat_percent = 4
  }
  feeds = @(
    @{ feed_id = "corn"; owned = $true },
    @{ feed_id = "wheat_bran"; owned = $true },
    @{ feed_id = "soybean_meal"; owned = $true },
    @{ feed_id = "rapeseed_meal"; owned = $true },
    @{ feed_id = "peanut_meal"; owned = $true },
    @{ feed_id = "alfalfa_hay"; owned = $true },
    @{ feed_id = "corn_stover"; owned = $true },
    @{ feed_id = "peanut_vine"; owned = $true },
    @{ feed_id = "sheep_grass"; owned = $true },
    @{ feed_id = "oat_hay"; owned = $true },
    @{ feed_id = "corn_silage"; owned = $true },
    @{ feed_id = "salt"; owned = $true },
    @{ feed_id = "limestone"; owned = $true }
  )
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Uri "https://你的域名/api/rations/calculate" -Method Post -ContentType "application/json" -Body $body
```

预期结果：

- `status = feasible`
- `qualified = true`
- 所有 `nutrient_status[].pass = true`

### 6.4 近似配比流程

只勾选玉米：

```powershell
$body = @{
  animal = @{
    class = "lactating"
    body_weight_kg = 50
    milk_kg = 2.5
  }
  feeds = @(
    @{ feed_id = "corn"; owned = $true }
  )
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Uri "https://你的域名/api/rations/calculate" -Method Post -ContentType "application/json" -Body $body
```

预期结果：

- `status = approximate`
- `qualified = false`
- `violations` 中列出具体未达标项
- 配方中只包含玉米，不会自动加入未勾选原料

### 6.5 AI 回退验证

在云托管未配置 `DEEPSEEK_API_KEY` 时，调用：

```text
POST https://你的域名/api/rations/calibrate
```

预期：

```json
{
  "status": "ok",
  "ai_unavailable": true,
  "approved": true
}
```

说明后端在无密钥时能正常回退，不影响确定性配方计算。

---

## 7. 微信小程序前端待办

本次交接只完成后端云托管准备，小程序前端尚未开始。后续开发应遵守：

- 小程序端只负责采集输入和展示结果，不在前端实现营养公式或优化器。
- 请求层使用 `wx.request`，API 地址配置为云托管 HTTPS 域名。
- 三个核心页面：
  1. 羊只信息页
  2. 原料选择页
  3. 计算结果页
- 必须保留三种颜色语义：
  - 绿色：合格 / 达标
  - 黄色：达标但贴边
  - 红色：未达标 / 暂不合格 / approximate 禁止饲喂
- AI 按钮只在 `status === "feasible"` 时显示。
- 手机端营养复核表需要横向滚动或卡片化显示。

---

## 8. 当前已知事项

1. 当前没有登录、数据库、配方历史保存。
2. 默认原料成分是估算值，不是实验室检测值。
3. 模型只覆盖宏量指标，不保证维生素、微量元素和可代谢蛋白完整满足。
4. AI 依赖云托管出网能力和 `DEEPSEEK_API_KEY`，不可用时自动回退。
5. 仓库公开后，所有提交历史对所有人可见；提交前必须再次确认没有密钥、手机号等敏感信息。

---

## 9. 上传完成后的交接确认清单

- [ ] `git status` 已检查，`.env` 未出现在待提交列表
- [ ] `git ls-files .env` 无输出
- [ ] 本地源码已提交并推送到 GitHub `main`
- [ ] GitHub 仓库已按要求设置为公开或授权云托管访问
- [ ] 微信云托管已成功拉取 `main` 分支
- [ ] 云托管使用 `./Dockerfile`，端口为 `8000`
- [ ] 云托管环境变量已配置 `DEEPSEEK_API_KEY`
- [ ] `GET /api/health` 返回 HTTP 200
- [ ] `/api/rations/calculate` 正常返回 `feasible`
- [ ] 少量原料场景正常返回 `approximate`
- [ ] 小程序后台已配置 HTTPS request 合法域名
