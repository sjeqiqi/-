# 奶山羊日粮配比助手｜项目交接文档

> 交接日期：2026-08-16  
> 当前版本：0.1.0（本地 MVP / 比赛展示版）  
> 适用对象：后续开发者、部署人员、产品/营养模型评审人员

## 1. 项目定位

这是一个面向小农户的奶山羊日粮配比工具。用户输入羊只情况、允许使用的原料和价格，系统在用户勾选的原料范围内，计算一份满足当前宏量营养模型的最低成本日粮。

核心原则：

- 未勾选的原料不会被后台自动加入。
- 最终原料用量按 10 g/只/天取整，并按取整后的用量重新复算。
- 程序确定性结果决定配方、营养状态、成本和边界提示。
- AI 只负责把已有事实讲得更口语，不得改配方、改数值或新增未经确认的饲养建议。
- 工具不是疾病诊断或治疗工具，不承诺提高产奶量，也不能据此认定为完整、长期、全价日粮。

## 2. 当前已完成的产品能力

### 首页与流程

- 首页直接说明用途：“输入羊只情况、原料和价格，自动计算推荐配方”。
- 推荐模式是默认主入口，自选模式是次级入口。
- 三步流程为：羊只信息 → 原料选择 → 计算结果。

### 羊只信息

- 成年泌乳奶山羊。
- 成年非泌乳维持期。
- 体重范围：25–90 kg。
- 泌乳期日产奶量范围：0.2–5.0 kg/d。
- 泌乳期乳脂率范围：2.0%–7.0%，可留空使用默认值。

### 原料选择

- 推荐模式默认勾选候选原料池。
- 自选模式完全尊重用户勾选集合。
- 原料按三组卡片展示：精料、粗饲料、矿物质。
- 每种原料可修改价格。
- 每种原料可选填检测值覆盖；留空使用默认成分估算值。
- 对原料过少、没有粗饲料、没有食盐等情况给出黄色预提示，但允许继续计算，让结果页明确列出缺口。

### 结果页

结果页顶部优先展示四张核心卡片：

- 每日喂料总量。
- 干物质。
- 日成本。
- 状态：配方合格 / 暂不合格。

后续内容分为：

1. 合格状态。
2. 营养复核。
3. 余量提醒。
4. 配方解读：配方特点、为什么这样配、使用建议、风险提醒。

颜色语义：

- 绿色：已达标或配方合格。
- 黄色：已达标但离边界较近，黄色提醒不影响合格判定。
- 红色：未达标、暂不合格或需要优先处理的错误。

## 3. 目录与职责

```text
backend/
  app/
    main.py          FastAPI 入口、API 路由、固定使用建议/风险文案、静态页面托管
    models.py        Pydantic 请求/响应模型
    service.py       请求预处理、原料勾选和输入校验
    nutrition.py     FCM、DMI、ME、CP、NDF、Ca、P 等营养需求公式
    optimizer.py     严格求解、近似求解、10 g 取整和复算
    insights.py      确定性来源贡献与 boundary_flags 生成
    ai.py            DeepSeek Flash 客户端、提示词、JSON 校验、安全回退
    feeds/v1.json    版本化原料目录与默认成分/价格
    spec.py          API、服务名、取整步长等常量
  tests/             pytest 测试

frontend/
  src/
    App.tsx                    三步页面状态与请求组装
    api.ts                     前端 API 客户端
    types.ts                   前后端数据类型
    components/StepAnimal.tsx  羊只信息
    components/StepFeeds.tsx   原料卡片、推荐/自选模式
    components/StepResult.tsx  结果卡片、复核、解读和 AI 按钮
    styles.css                 桌面端和手机端样式
    __tests__/App.test.tsx     前端交互测试
```

## 4. 后端数据流

```text
POST /api/rations/calculate
  → Pydantic 校验
  → prepare_request（处理原料、价格、检测值覆盖）
  → optimize_ration（严格路径）
  → 10 g 取整
  → 取整后完整营养复算
  → 必要时进入 approximate 近似路径
  → build_ration_insights
  → 返回结果
```

### 返回状态

- `feasible`：严格可行，取整后复核仍达标，`qualified=true`。
- `approximate`：当前勾选原料内无法同时满足全部约束，返回营养缺口最小的近似配比，`qualified=false`，禁止直接作为合格日粮使用。
- `infeasible`：求解器无法形成可返回的结果，返回结构化原因和建议。

### 当前模型覆盖

- 4% FCM / 3.5% FCM。
- DMI 目标与允许带宽。
- ME（含安全余量）。
- CP 分档下限/上限。
- NDF。
- 粗饲料比例下限。
- Ca、P、Ca:P。
- 食盐固定比例。
- 单项原料用量上限。

默认原料成分是公开资料估算值，不是本批次实验室检测值。用户有检测数据时，应通过原料卡片的检测值覆盖重新计算。

## 5. `ration_insights` 与边界提示

`backend/app/insights.py` 只读取最终配方结果，不参与求解，也不修改 `status`、`qualified` 或营养判定。

它负责生成：

- 已勾选原料数量。
- 最终实际使用原料数量。
- 总干物质与粗饲料干物质比例。
- 主要能量来源及贡献比例。
- 主要粗蛋白来源及贡献比例。
- 已达标但余量较小的 boundary flags。
- 固定模型范围说明 `scope_notice`。

boundary flags 只是解释层提示。营养复核表和后端确定性判定优先级高于黄色提示。

## 6. AI 解读边界

### 调用方式

```text
POST /api/rations/calibrate
```

该接口会先执行确定性计算，只有 `feasible` 结果才调用 AI。近似结果和不可行结果不会生成 AI 校准解读。

当前 AI 配置：

- 模型：`deepseek-v4-flash`。
- Base URL：`https://api.deepseek.com`。
- 超时：30 秒。
- 密钥变量：`DEEPSEEK_API_KEY`。

项目根目录的 `.env` 只用于本地配置，已经被 Git 忽略；文档和源码中不记录真实密钥。后端当前不会自动解析 `.env` 文件，启动 uvicorn 前需要把变量注入当前进程环境，或使用部署平台的环境变量配置。

### AI 能做什么

- 基于 `nutrient_status`、`ration_insights`、固定建议和风险事实进行口语化转写。
- 用短句、结论先行的方式解释来源、达标状态、贴边原因和使用注意事项。
- 首次出现专业词时，用括号补充中文解释。

### AI 不能做什么

- 不能修改任何原料克数、需求量、约束、价格或成本。
- 不能把已达标指标说成不足或超标。
- 不能把宏量指标达标说成完整营养已满足。
- 不能新增具体换料天数、每日饲喂次数或某种原料增减克数。
- 不能建议用户手工调整配方；输入变化时只能建议更新输入后重新求解。
- 不能做疾病诊断、治疗建议或产奶量承诺。

`ai.py` 会校验返回 JSON 结构，并拦截具体天数/次数、动态调方、增减克数等不安全表述。超时、网络错误、Key 缺失、非法 JSON 或内容不合规时，返回本地固定说明，确定性结果不受影响。

## 7. 本地运行与部署

### 后端安装

```powershell
cd backend
python -m venv .venv
.\.venv\Scripts\pip install -r requirements-dev.txt
```

### 开发模式

终端 1：

```powershell
cd backend
$env:PYTHONUTF8 = "1"
.\.venv\Scripts\python -X utf8 -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

终端 2：

```powershell
cd frontend
npm install
npm run dev
```

访问 `http://127.0.0.1:5173/`。Vite 会把 `/api` 代理到 `127.0.0.1:8000`。

### 单服务生产预览

```powershell
cd frontend
npm run build
cd ..\backend
.\.venv\Scripts\python -X utf8 -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

访问 `http://127.0.0.1:8000/`。只要 `frontend/dist` 存在，FastAPI 会托管生产构建结果。

启用 AI 时，启动 uvicorn 前设置 `DEEPSEEK_API_KEY`。不要把真实 Key 写入源码、README、测试或提交记录。

### 健康检查

```powershell
Invoke-WebRequest http://127.0.0.1:8000/api/health -UseBasicParsing
```

预期 HTTP 200，响应包含 `status: "ok"`。

## 8. 测试与当前验收结果

后端：

```powershell
cd backend
.\.venv\Scripts\python -X utf8 -m pytest -q
```

当前结果：**75 passed**。包含公式、优化、API、边界提示、AI 回退和 AI 不安全文案拦截测试。

前端：

```powershell
cd frontend
npm test -- --run
npm run build
```

当前结果：**16 passed**，生产构建通过。前端测试覆盖首页入口、三步流程、推荐/自选模式、原料卡片、结果核心卡、黄色提示和 AI 回退。

仓库原 README 中的测试数量是旧版本记录（54/9），以本交接文档和实际命令输出为准。

## 9. 已知限制与风险

- 当前没有登录、权限、数据库或历史配方保存能力，数据只存在于当前页面状态。
- 默认原料成分为估算数据；不同批次原料可能导致边界附近指标变化。
- 当前模型只覆盖宏量指标，不保证维生素、微量元素和可代谢蛋白完整满足。
- AI 依赖外部网络和 Key；AI 不可用时会回退，不影响确定性计算。
- 前端已加入手机端响应式规则、触控尺寸和表格横向滚动，但发布前仍应在实际手机上做一次视觉验收。
- 当前工作区包含一批已有但尚未提交的后端、前端和说明文档改动。正式交接前应由负责人确认 `git diff`，整理提交边界并创建版本标签。
- `frontend/dist` 是构建产物，不应手工修改；修改源码后重新执行 `npm run build`。

## 10. 后续建议

优先级建议如下：

1. 用营养专家确认固定使用建议和风险文案，形成有来源的产品知识版本。
2. 选真实手机完成首页、原料卡片、结果表格和 AI 解读的视觉验收。
3. 为 `.env` 和生产环境变量建立正式部署说明，避免启动时漏注入 Key。
4. 增加一次真实 DeepSeek Flash 调用的人工验收，但不要把真实 Key 写进测试。
5. 如需长期使用，再考虑用户、配方保存、原料批次管理和检测报告导入。

## 11. 交接前检查清单

- [ ] 确认没有把真实 `DEEPSEEK_API_KEY` 写入 Git。
- [ ] 执行后端 75 项测试。
- [ ] 执行前端 16 项测试和生产构建。
- [ ] 检查 `http://127.0.0.1:8000/api/health` 返回 200。
- [ ] 手动走一遍推荐模式完整流程。
- [ ] 手动走一遍自选少量原料的 approximate 流程。
- [ ] 确认黄色提醒不会被误读为不合格。
- [ ] 确认 AI 解读失败时，四个确定性区块仍然有内容。
- [ ] 确认 AI 输出没有具体换料天数、饲喂次数或手工改克数建议。
- [ ] 确认手机端按钮、输入框和结果表格可操作。
